# ───────────── 跟踪白名单 / 黑名单规则（2026-09-11 用户整理后）─────────────
# 白名单（跟踪）: docs/  scripts/  tools/  build/（除 master-build/staging/bin）  + 根 README.md
# 黑名单（忽略）: src/  assets/  release/  build/master-build/  build/staging/  build/bin/  + 所有 *.bak*
#
# 注意：gitignore 不支持行内尾随注释（# 与模式同行会被视为模式），
#       所以注释一律独立成行，不跟在模式后面。

# ── 黑名单：大型/产物/备份目录，一律不入库 ──
/src/
/assets/
/release/
/build/bin/
/build/master-build/
/build/staging/
/build/.inspect*/
/build/.slim-test/
/build/tmpcheck/
/build/spk-check*/
/build/tmp/
/build/.trash*/
/build/fix-nas2/
*.bak*

# ── 敏感/运行时文件（含密码/凭据，严禁入库） ──
/web-install/install-config.json
/web-install/install-tasks.jsonl
/web-install/install-server.log
/web-install/server-install.log
/scripts/release-sync.log
/scripts/server-install.log
/DSHProbe-*.spk
/__pycache__/
*.pyc

# ── 构建时生成的 pnpm 垫片（含本机绝对路径，由 build-common.sh 自动生成） ──
/tools/pnpm/bin/pnpm

# ── 本地回收站（安全删除暂存） ──
/.trash/

# ── 任务清单（插话记录，不入库） ──
/task.md
/任务.md

# ── 外部研究仓库（独立 git，移出项目根，禁止再 add -A 扫入） ──
/research-*/

# ── 操作系统 / 编辑器杂项 ──
.DS_Store
Thumbs.db
*.swp
*.swo
*~
node_modules/
node_modules.orig/
