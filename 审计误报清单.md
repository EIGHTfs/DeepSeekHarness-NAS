# 审计误报清单（dsh-git-push audit）

> 2026-09-16 全量审计（`code_audit` / `git-sluice audit --full`）结果 57.3/C、879 条 findings 中，
> 经逐条人工核实，以下为**误报 / 第三方产物误计**，不应视为本项目代码质量问题。
> 后续重跑审计时对照本清单判断，避免重复「修」误报。

## 一、误报（规则过度匹配，实际无问题）

### 1. scripts/sync-github-release.sh —— `TOKEN=` 赋值被当硬编码凭据

| 行 | 规则 | 实际内容 | 误报原因 |
|----|------|----------|----------|
| 33 | `secret-generic-token` / `security/no-hardcoded-credentials` | `TOKEN="$GITHUB_TOKEN"` | 引用**环境变量**，非硬编码字符串 |
| 40 | `secret-generic-token` / `security/no-hardcoded-credentials` | `TOKEN="$(python3 -c "...open(sys.argv[1])..." "$p")"` | 从**凭据文件读取**（`$HOME/.dsh/git-push/github-token` 等），凭据本身不在代码里 |
| 84 | `security/no-hardcoded-credentials` | `TOKEN="$TOKEN" REPO=... python3 -` | **环境变量透传给子进程**，非硬编码 |

处置：不改代码（凭据读取逻辑正确，符合凭据托管约定）；规则对 `TOKEN=` 赋值模式过度匹配，
如需消除告警可在命令行豁免，但**不应为了过审计把凭据读取改坏**。

### 2. 官方文档里的示例邮箱被当隐私泄漏

| 文件 | 行 | 规则 | 实际内容 | 来源 |
|------|----|------|----------|------|
| `docs/官方文档/spk/toolkit__sign_package.md` | 68 | `secret-email` | `Synology Inc. <synology_inc@synology.com>` | 群晖**官方签名文档**里的示例 GPG key uid，全网公开 |
| `tools/fnpack-usage.md` | 3 | `secret-email` | `Maintainer anna <nas@teiron-inc.cn>` | 飞牛 fnOS **官方 fnpack 工具的 dpkg Maintainer** 信息，非本机用户邮箱 |

处置：不改（引用官方公开信息，无隐私风险）。

### 3. web-install/install.html —— 管理页内联脚本被当 XSS 风险

| 行 | 规则 | 实际内容 | 误报原因 |
|----|------|----------|----------|
| 215 | `security/script-unsafe-inline` | 单 `<script>` 块内联整页 JS | 独立管理页 SPA 的常规写法；规则提示是「任何 XSS 都可能严重」的**通用告警**，非已定位漏洞 |

补充：页面 `innerHTML` 渲染列表处，`t.note` 已做 `&quot;` 转义、`t.package` 已做单引号 escape，
其余字段（ts/version/system）来自本地 `install-tasks.jsonl`（仅本机管理员可写），风险面低。
如需进一步加固，可加统一 HTML escape 函数，见「真实问题」章节。

## 二、第三方产物（应豁免，不计入项目分）

### 4. tools/pnpm/dist/ —— pnpm 打包产物（共 475 warning + 7 blocker）

- 文件：`tools/pnpm/dist/worker.js`（12,236 行、432.9KB 单文件 bundle，pnpm 11.25.0）
- 命中规则：`readability/magic-number*`(152)、`max-nesting-depth`(83)、`variable-min-length`(52)、
  `max-function-length`(7 blocker+8 warning)、`security/no-eval`(1 blocker)、`file-health`(1 blocker) 等
- **性质**：pnpm 官方工具链的打包产物（esbuild/rollup bundle），`build-common.sh` 强制使用
  （`PNPM_BIN="$WS/tools/pnpm/bin/pnpm.mjs"`），CI 与本地构建都依赖它，**不能删**；
  但其代码风格为工具生成，非本项目手写，**不应计入项目评分**
- 处置：建议审计按 `**/vendor/**` 或第三方产物目录排除；git 已跟踪 457 个 dist 文件，
  不 git rm（构建链引用 `tools/pnpm/dist/pnpm.mjs`）

### 5. docs/官方文档/ —— 下载的官方文档（26 warning）

- `docs/官方文档/fpk/*.md` 等 119 个文件，由 `scripts/fetch-official-docs.py` 下载的飞牛官方文档
- 命中规则：`robustness/no-sync-fs`(15)、`readability/vague-variable-name`(4) 等——文档**示例代码**里的
  fs 操作/变量名，非本项目运行代码
- 处置：建议审计排除该目录，或明确其为「官方文档留档」（`.samples` 之外再设豁免）

## 三、真实问题（应修，占剩余 422 条）

排除误报与第三方后，项目的真实问题集中在：

- **scripts/dsh-repair.js（79）+ dsh-repair.cjs（47）**：两个同功能修复脚本，含 168~196 行超长函数
  （`max-function-length` blocker ×2）、大量 magic number / 嵌套过深 / 空 catch——建议合并为单一权威
  脚本并拆分函数（本项目后续重构项）
- **web-install/install-remote-spk.sh（34）+ build-fpk.sh（32）+ build-spk.sh（32）**：shell 脚本的
  `magic-number`（端口/阈值字面量）、`variable-min-length`（单字母变量）、`max-nesting-depth`
- **其余**：`web-install/install.html`(44)、`build-common.sh`(20)、`install-server.py`(14) 等同类风格项

处置：这些是真问题，按「脚本常量区 + 命名规范 + 函数拆分」逐步修；但**不应为了分数批量改写
已验证可用的打包/安装链路**（改动需重新实机验证），建议排期按模块推进。

## 附：审计范围建议（避免重复误报）

1. `tools/pnpm/dist/`（第三方 bundle）
2. `docs/官方文档/`（下载的官方文档）
3. `.samples` 目录（已豁免机制支持）

如审计插件支持 `ruleset` 自定义排除，将上述目录加入；不支持时对照本清单人工判断。